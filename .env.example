# Copy file này thành .env (không commit .env thật — xem .gitignore)
# Môi trường: dev hoặc prod
ENVIRONMENT=dev
API_BASE_URL=https://api.dev.vstech-hrm.example.com
# Bật chế độ Demo độc lập (Mock Data không cần Backend)
USE_MOCK_DATA=true
